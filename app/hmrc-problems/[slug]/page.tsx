import type {Metadata} from 'next';
import Link from 'next/link';
import {notFound} from 'next/navigation';
import {problems} from '../problems';
import '../../knowledge-base/knowledge.css';

export function generateStaticParams(){return problems.map(problem=>({slug:problem.slug}))}

export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{
  const{slug}=await params;const problem=problems.find(item=>item.slug===slug);if(!problem)return{};
  return{title:{absolute:`${problem.title} | TaxEnquiryHelp`},description:problem.description,alternates:{canonical:`/hmrc-problems/${problem.slug}`},openGraph:{title:problem.title,description:problem.description,url:`/hmrc-problems/${problem.slug}`,type:'article',locale:'en_GB',siteName:'TaxEnquiryHelp'},twitter:{card:'summary_large_image',title:problem.title,description:problem.description}};
}

export default async function ProblemPage({params}:{params:Promise<{slug:string}>}){
  const{slug}=await params;const problem=problems.find(item=>item.slug===slug);if(!problem)notFound();
  const url=`https://www.taxenquiryhelp.co.uk/hmrc-problems/${problem.slug}`;
  const related=problems.filter(item=>item.slug!==problem.slug&&item.category===problem.category);
  const article={'@context':'https://schema.org','@type':'Article',headline:problem.title,description:problem.description,datePublished:'2026-07-23',dateModified:'2026-07-23',author:{'@type':'Organization',name:'TaxEnquiryHelp Editorial'},publisher:{'@type':'Organization',name:'TaxEnquiryHelp'},mainEntityOfPage:url};
  const crumbs={'@context':'https://schema.org','@type':'BreadcrumbList',itemListElement:[{'@type':'ListItem',position:1,name:'Home',item:'https://www.taxenquiryhelp.co.uk'},{'@type':'ListItem',position:2,name:'HMRC Problems',item:'https://www.taxenquiryhelp.co.uk/hmrc-problems'},{'@type':'ListItem',position:3,name:problem.title,item:url}]};
  const faq={'@context':'https://schema.org','@type':'FAQPage',mainEntity:problem.faqs.map(([name,text])=>({'@type':'Question',name,acceptedAnswer:{'@type':'Answer',text}}))};
  return <main className="kb article-page question-page"><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(article)}}/><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(crumbs)}}/><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(faq)}}/><div className="kb-wrap"><nav className="breadcrumbs" aria-label="Breadcrumb"><Link href="/">Home</Link><span>›</span><Link href="/hmrc-problems">HMRC Problems</Link><span>›</span><span>{problem.category}</span></nav><article className="guide-layout"><div className="guide-main"><header className="guide-header"><div className="guide-label">{problem.category}</div><h1>{problem.title}</h1><p className="guide-dek">{problem.description}</p><div className="byline"><span>By TaxEnquiryHelp Editorial</span><span>Reviewed 23 July 2026</span></div></header><div className="key-answer"><b>Direct answer</b><p>{problem.directAnswer}</p></div><nav className="contents" aria-label="On this page"><b>On this page</b><ol>{problem.sections.map((section,index)=><li key={section.title}><a href={`#section-${index+1}`}>{section.title}</a></li>)}<li><a href="#actions">Action checklist</a></li><li><a href="#example">{problem.example.title}</a></li><li><a href="#faqs">Frequently asked questions</a></li></ol></nav>{problem.sections.map((section,index)=><section className="guide-section" id={`section-${index+1}`} key={section.title}><div className="guide-section-heading"><span>{String(index+1).padStart(2,'0')}</span><h2>{section.title}</h2></div>{section.paragraphs.map(paragraph=><p key={paragraph}>{paragraph}</p>)}{section.bullets&&<div className="guide-checklist"><b>Common areas</b><ul>{section.bullets.map(item=><li key={item}>{item}</li>)}</ul></div>}</section>)}<section className="guide-section" id="actions"><div className="guide-section-heading"><span>05</span><h2>What to do now</h2></div><div className="guide-checklist"><b>Action checklist</b><ol>{problem.actions.map(item=><li key={item}>{item}</li>)}</ol></div><div className="guide-checklist"><b>Records to gather</b><ul>{problem.records.map(item=><li key={item}>{item}</li>)}</ul></div></section><section className="guide-section" id="example"><div className="guide-section-heading"><span>06</span><h2>{problem.example.title}</h2></div>{problem.example.paragraphs.map(paragraph=><p key={paragraph}>{paragraph}</p>)}</section><section className="guide-faq" id="faqs"><h2>Frequently asked questions</h2>{problem.faqs.map(([name,text])=><details key={name}><summary>{name}<span>+</span></summary><p>{text}</p></details>)}</section><section className="sources"><h2>Official sources</h2><p>Check the current source and the exact HMRC document before acting.</p><ul>{problem.sources.map(([name,href])=><li key={href}><a href={href} rel="external">{name} ↗</a></li>)}</ul></section><aside className="medical-note"><b>Important:</b> General information only, not tax or legal advice. The correct response depends on the notice, tax, period, facts and deadlines.</aside></div><aside className="guide-side"><div className="help-box"><span>Need specialist support?</span><h2>Tell us what HMRC sent.</h2><p>The relevant enquiry category will already be selected.</p><Link href={`/?category=${problem.enquiryCategory}#enquiry`}>Start a confidential enquiry →</Link></div><div className="related"><b>Complete guide</b><Link href={problem.canonicalGuide.href}>{problem.canonicalGuide.title}<span>→</span></Link></div>{related.length>0&&<div className="related"><b>Related problems</b>{related.map(item=><Link key={item.slug} href={`/hmrc-problems/${item.slug}`}>{item.title}<span>→</span></Link>)}</div>}</aside></article></div></main>;
}
